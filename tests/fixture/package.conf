# Hermetic fixture: the upstream repository is created by tests/fixture-build.sh
# inside the mount, so no network is involved.
UPSTREAM=file:///target/upstream
VERSION=v0.0.1
TOOLCHAIN=none
PRE_BUILD_RULES=0
DBGSYM=0
